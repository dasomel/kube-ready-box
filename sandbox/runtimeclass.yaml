apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
overhead:
  podFixed:
    cpu: "10m"
    memory: "16Mi"
scheduling:
  nodeSelector:
    kube-ready-box/sandbox: "true"
  tolerations:
  - key: kube-ready-box/sandbox
    operator: Exists
    effect: NoSchedule
